# Goal: AI Agent-Friendly Repository Analyzer

## 목적

정적 분석으로 AI 코딩 에이전트가 저장소를 탐색하고 변경할 때 불필요하게 많은 문맥과 토큰을 소비하게 만드는 구조를 찾는다. 주 사용자는 저장소 관리자다. 관리자가 진단 근거를 확인하고 저장소 구조를 개선한 뒤 같은 분석을 다시 실행해 효과를 비교할 수 있어야 한다.

결과는 단순한 코드 복잡도 순위가 아니라 다음 질문에 답해야 한다.

- 어떤 파일이나 심볼을 이해하려면 너무 많은 주변 코드를 읽어야 하는가?
- 탐색이 어디에서 과도하게 분기되거나 반복되는가?
- 어떤 구조가 관련 코드를 찾기 어렵게 하거나 잘못된 대상으로 유도하는가?
- 작은 변경이 넓은 문맥 확인과 검증을 요구하는 지점은 어디인가?
- 문서, 테스트, 타입, 프레임워크 지식 중 무엇을 보강하면 탐색 비용이 가장 크게 줄어드는가?

이 문서에서는 측정 대상을 **저장소 탐색 비용**으로 부른다. **에이전트 친화성**은 작업 대상을 찾고 영향 범위를 확인하는 데 필요한 탐색 비용과 불확실성이 낮은 성질을 뜻한다.

## 현재 상태

| 상태 | 범위 |
|---|---|
| 구현됨 | Python 및 TypeScript/JavaScript 파일·심볼 그래프와 import, call, inheritance 등 언어 관계 |
| 구현됨 | FastAPI route, dependency injection, schema 및 Android Compose, DI, Room, Retrofit 의미 분석 |
| 구현됨 | PageRank, HITS, degree/betweenness centrality, 소스 토큰 비용, weighted centrality cost, 2/3-hop 비용 |
| 구현됨 | 엣지 신뢰도·해소 상태, Git working tree 또는 commit 간 architecture diff, JSON·Mermaid·HTML 출력 |
| 구현됨 | Kotlin 언어 그래프, 테스트·설정 연결, 생성·vendor·migration 비용 정책 |
| 구현됨 | 결정적 sampled betweenness와 단일 BFS 2/3-hop 비용 계산 |
| 구현됨 | 작업 seed retrieval, BFS·가중 최단 경로·예산 제한 탐색, 경로별 비용과 종료 이유 리포트 |
| 구현됨 | 중심 대형 심볼, 브리지 병목, re-export·동일 이름 모호성, 순환 의존, 테스트 연결 누락 진단과 근거 경로 |
| 구현됨 | 두 분석 상태 사이의 노드 비용·진단·작업 비용 diff |
| 검증할 가설 | 진단의 오탐률, 문서·설정 연결, Git co-change, 실제 agent trace 보정 |

Git architecture diff는 두 상태의 구조 변화를 비교하는 현재 기능이다. 여러 변경 이력에서 반복적으로 함께 바뀌는 대상을 찾는 Git co-change 분석과는 구분한다.

## 핵심 가설과 분석 모델

에이전트 친화성은 개별 파일의 크기나 순환 복잡도만으로 설명되지 않는다. 에이전트가 진입점에서 작업 대상과 영향 범위를 확신할 때까지 거치는 **탐색 경로의 비용, 분기, 불확실성**을 함께 측정해야 한다.

따라서 저장소를 여러 관계가 겹친 희소 속성 그래프로 표현한다.

- 노드: 파일, 모듈, 클래스, 함수, 메서드, 타입, 설정, 테스트, 문서, API 엔드포인트
- 정적 관계: import, call, inheritance, type use, read/write, instantiate, registration
- 프레임워크 관계: route-to-handler, dependency injection, middleware, event hook, serializer/model binding
- 보조 관계: test-to-production, document-to-symbol, configuration-to-consumer, Git co-change
- 노드 비용: 실제 또는 추정 토큰 수, 코드 크기, 구문 복잡도, 요약 가능성
- 엣지 신뢰도: 정적 확정, 정적 추정, 프레임워크 추론, 동적 확인 필요

FastAPI와 Android는 프레임워크가 숨기는 연결을 언어 그래프 위에 보강하는 기준 사례다. 최종 제품의 중심은 특정 프레임워크가 아니라 언어 수준의 분석 코어와 교체 가능한 프레임워크 의미 해석 계층이다.

## 지표 계약

| 지표 | 정의 | 해석 | 상태 및 한계 |
|---|---|---|---|
| Token cost | 심볼의 소스 범위에 대한 토큰 수 또는 문자 수 기반 근사 | 대상을 한 번 읽는 비용 | 구현됨. 모델별 tokenizer와 실제 부분 읽기를 반영하지 않음 |
| Weighted centrality cost | `PageRank × token cost` | 반복적으로 필요하면서 읽기에도 비싼 대상 | 구현됨. 작업별 중요도가 아닌 전체 그래프 중심성 사용 |
| Hub/authority score | 방향 그래프에 대한 HITS 점수 | 탐색을 분배하는 허브와 참조가 모이는 핵심 대상 | 구현됨. 높은 점수 자체를 결함으로 판정하지 않음 |
| Betweenness bottleneck | 방향 최단 경로가 특정 노드를 통과하는 비율 | 서로 다른 영역 사이의 잠재적 탐색 병목 | 구현됨. 큰 그래프에서는 계산 비용이 큼 |
| k-hop context cost | 시작 노드를 제외하고 k-hop 안에서 도달하는 고유 노드의 token cost 합 | 주변 문맥을 모두 확인하는 상한에 가까운 비용 | 2/3-hop 구현됨. 관련 없는 이웃도 포함 |
| Branching burden | 탐색 단계마다 노출된 후보 수와 라벨 기준 비관련 후보 비율 | 관련 경로를 고르기 어려운 정도 | 구현됨. 정답 라벨 또는 검증된 trace 필요 |
| Target discovery cost | seed부터 정답 구현 지점을 찾을 때까지 읽은 고유 노드의 누적 token cost | 수정 지점 발견 비용 | 구현됨. 탐색 정책과 정답 라벨에 의존 |
| Impact discovery cost | 변경 대상부터 라벨된 소비자·영향 파일·테스트를 종료 조건까지 찾는 누적 비용 | 변경 영향 확인 비용 | 구현됨. 요구 recall에 따라 값이 달라짐 |
| Context fragmentation | 정답 경로에 필요한 고유 파일·디렉터리 수와 이들 사이의 그래프 거리 | 필요한 근거의 분산 정도 | 구현됨. 저장소 크기별 정규화 필요 |
| Backtracking risk | ambiguous·re-export·unresolved 경로의 노출량과 trace에서 잘못 선택한 분기 수 | 잘못된 대상을 따라갔다가 되돌아갈 위험 | `reexport_ambiguity` 진단으로 정적 신호 집계됨. 단일 위험 점수와 trace 검증은 계획됨 |
| Exploration cost diff | 두 분석 상태 사이의 노드 비용·중심성·작업별 발견 비용 변화 | 개선 조치가 실제로 탐색 비용을 줄였는지 | 구현됨. 노드 동일성이 id·symbol_path·(kind, label, 경로) 매칭에 의존 |
| Evidence gap | 선택 경로에서 `dynamic_required`, `ambiguous`, `unresolved` 관계가 차지하는 비율과 수 | 정적 분석 외 검색이나 실행이 필요한 정도 | 경로 단위 집계까지 구현됨 |

지표는 하나의 종합 점수로 저장소의 좋고 나쁨을 판정하지 않는다. 작업 유형, 근거 경로, 엣지 신뢰도, 오탐 가능성을 함께 제시한다.

## 작업 모델 계약

작업 기반 탐색은 seed, 정답 집합, 종료 조건을 명시한 뒤 실행한다.

| 작업 유형 | Seed 예시 | 정답 집합 | 종료 조건 |
|---|---|---|---|
| 버그 수정 | 오류 메시지, stack trace, 실패 테스트 | 결함 심볼, 관련 호출자, 회귀 테스트 | 결함 심볼을 찾고 요구된 영향 범위와 테스트를 확인 |
| 기능 추가 | 기능 설명, API 경로, 기존 유사 심볼 | 확장 지점, 관련 타입·설정, 테스트 | 구현 지점과 준수할 인터페이스 및 검증 지점을 확인 |
| API 변경 | URL, endpoint, 공개 심볼 | 정의, schema, 소비자, 테스트 | 라벨된 소비자와 호환성 검증 지점을 요구 recall까지 확인 |
| 설정 변경 | 설정 키, 환경 변수 | 정의, 기본값, 소비자, 검증 코드 | 모든 라벨된 소비자와 관련 테스트를 요구 recall까지 확인 |

탐색은 import, call, type, test, 프레임워크 관계의 신뢰도와 작업 관련성, 노드 비용을 이용해 frontier를 선택한다. 구현 지점, 영향 범위, 관련 테스트가 작업별 종료 조건을 충족하면 끝난다. BFS, 가중 최단 경로, 확률적 탐색 정책을 같은 작업 세트에서 비교한다.

## 구조적 마찰 진단 후보

- 여러 책임이 모인 중심 대형 파일 또는 god symbol
- 과도한 barrel/re-export와 동일·유사 이름으로 인한 심볼 해소 모호성
- 동적 import, reflection, monkey patching, 문자열 기반 등록처럼 정적 연결이 끊기는 지점
- 정의와 등록, 설정과 소비, 구현과 테스트가 멀리 떨어진 구조
- 공통 유틸리티 허브가 관련 없는 도메인을 연결하는 구조
- 순환 의존과 양방향 호출로 탐색 종료 지점이 불명확한 구조
- 프로덕션 코드와 테스트의 연결 누락 또는 지나치게 큰 통합 테스트만 존재하는 구조
- 실제 구조와 README, 경로명, 타입 힌트가 불일치하는 경우
- 생성 코드, vendor 코드, migration처럼 일반 작업에서 우선순위가 낮은 영역이 탐색을 오염시키는 경우

진단 결과는 곧바로 리팩터링 결론으로 취급하지 않는다. 예상 절감 토큰은 실제 trace로 보정할 수 있을 때만 실험 지표로 제공한다.

## 계층 모델

1. **언어 코어**: AST, 심볼 테이블, import/call/type 그래프, 소스 범위와 토큰 비용
2. **프레임워크 어댑터**: 일반 언어 의미만으로 복원하기 어려운 route, registration, dependency injection 등의 연결
3. **저장소 보강기**: 테스트 연결, 문서 링크, 설정 소비, Git co-change
4. **작업 모델**: 작업별 seed, 정답 집합, 탐색 정책, 종료 조건
5. **진단 및 표현**: 비용이 큰 경로, 병목, 개선 후보와 근거를 JSON 및 대화형 리포트로 제공

언어 계층은 프레임워크 계층에 의존하지 않는다. 프레임워크 어댑터는 언어 그래프 위에 의미를 보강하고, 범용 지표와 출력은 특정 프레임워크의 어휘를 요구하지 않아야 한다.

## 저장소 관리자 작업 흐름

1. 저장소 또는 비교할 두 상태를 분석한다.
2. 상위 탐색 비용과 구조적 마찰 후보를 작업 유형별로 확인한다.
3. 진단의 근거 경로, 신뢰도, 오탐 가능성을 검토한다.
4. 진입점 문서화, 이름 변경, 타입 보강, re-export 축소, 테스트 연결 개선 등 적절한 조치를 선택한다.
5. 같은 설정과 작업 세트로 재분석해 탐색 비용과 진단 근거의 변화를 비교한다.

## 구현 마일스톤

### M0. 범용 그래프와 기준 어댑터 — 완료

구현 범위:

- Python 및 TypeScript/JavaScript 파일·심볼 그래프
- FastAPI 및 Android 프레임워크 의미 분석
- 공통 노드·엣지 모델, 신뢰도와 해소 상태
- 중심성, 토큰 비용, weighted cost, 2/3-hop 비용
- Git architecture diff와 JSON·Mermaid·HTML 출력

완료 조건: 지원 언어와 프레임워크의 샘플 저장소에서 그래프를 생성하고 범용 지표와 리포트를 동일한 파이프라인으로 출력한다.

### M1. 그래프 완성도와 비용 모델 — 완료

구현 범위:

- Python/Kotlin/TypeScript 관계의 누락 범위 정리와 type use, read/write, instantiate 관계 보강
- test-to-production, configuration-to-consumer 관계
- 생성 코드, vendor, migration 등 분석 제외 및 비용 감쇠 규칙
- 대규모 그래프의 betweenness와 neighborhood 계산 전략
- 관계 종류별 신뢰도와 unresolved·ambiguous 근거 출력

완료 조건: 정의된 fixture에서 계약된 관계와 신뢰도 상태를 재현하고, 제외 규칙 적용 전후의 그래프와 비용 차이를 설명할 수 있다.

### M2. 작업 기반 탐색 — 완료

구현 범위:

- URL, 심볼명, 오류 문자열, 설정 키, 변경 파일에서 seed를 찾는 retrieval
- 버그 수정, 기능 추가, API 변경, 설정 변경 작업 계약
- BFS, 가중 최단 경로, 예산 제한 탐색 정책
- Branching burden, target/impact discovery cost, context fragmentation, evidence gap 집계
- 구현 지점, 영향 범위, 테스트 발견 경로 리포트

완료 조건: 라벨된 작업마다 동일한 seed와 정답 집합으로 정책을 재실행할 수 있고, 종료 이유와 누적 비용을 경로 단위로 설명한다.

### M3. 구조적 마찰 진단과 개선 비교 — 완료

구현 범위:

- 중심 대형 심볼, 브리지, re-export 모호성, 순환 의존, 테스트 연결 누락 탐지
- 진단별 근거 경로, 적용 작업 유형, 신뢰도, 오탐 가능성
- 개선 후보와 관련 지표의 연결
- 변경 전후 또는 브랜치 간 저장소 탐색 비용 diff

완료 조건: 각 진단이 재현 가능한 그래프 근거를 제공하고, 변경 전후 같은 작업 세트에서 어떤 비용이 변했는지 보여 준다.

판정은 저장소 내 분포의 상대 분위수와 절대 하한을 함께 넘긴 대상만 진단한다. 해소된 컷 값을 리포트에 남겨 같은 입력에서 같은 결과를 재현한다. 비용 비교는 내보낸 두 분석 JSON을 입력으로 받고, 노드 동일성은 id, `symbol_path`, `(kind, label, 파일 경로)` 순으로 판정한다.

### M4. 실제 에이전트 행동 검증 — 계획됨

구현 범위:

- 공개 저장소의 작업, 정답 파일, 영향 파일, 테스트 benchmark
- 허용된 환경에서 파일 열기, 검색, 토큰 사용, backtracking trace 수집과 익명화
- 예측 비용과 실제 토큰, 도구 호출 수, 해결 시간, 성공률 비교
- 지표와 탐색 정책의 ablation 및 저장소·언어·작업 유형별 분석

평가 지표:

- 예측 비용과 실제 비용의 순위상관
- 정답 파일 및 영향 파일 recall@k와 precision/recall
- 예측 비용 오차
- 상위 구조적 마찰 진단의 저장소 관리자 확인율
- 개선 전후 동일 작업의 실제 탐색 비용 변화

완료 조건: 첫 benchmark baseline을 만들고 어떤 지표가 실제 탐색 비용을 유의미하게 설명하는지 보고한다. 합격 임계값은 baseline 결과를 검토한 뒤 별도로 결정한다.

### M5. 이력·문서 보강과 프레임워크 확장 — 계획됨

구현 범위:

- Git co-change와 정적 의존 관계의 불일치 탐지
- document-to-symbol 연결과 문서·코드 불일치 신호
- Django, Flask, Typer/Celery 및 추가 언어·프레임워크 어댑터 검증
- 어댑터 최소 인터페이스와 호환성 계약 확정

완료 조건: 새 보강기나 어댑터가 언어 코어와 범용 분석 계층을 변경하지 않고 연결되며, 기존 benchmark와 리포트 계약을 재사용한다.

## 검증 원칙

- 여러 저장소에 동일한 작업 세트를 적용하고 정답 파일, 영향 파일, 테스트를 라벨링한다.
- 저장소 크기, 언어, 프레임워크, 작업 유형별로 결과를 분리한다.
- 휴리스틱별 ablation으로 각 신호의 추가 예측력을 확인한다.
- 높은 비용으로 예측한 경로가 실제 탐색에서도 반복적으로 비싼지 확인한다.
- 개선 제안 적용 전후에 같은 작업을 재실행해 비용 감소를 확인한다.
- 도구는 점수뿐 아니라 그 이유를 재현 가능한 경로로 설명해야 한다.

## 비목표와 주의점

- 전통적인 코드 품질, 보안, 런타임 성능 분석을 대체하지 않는다.
- 큰 파일이나 높은 중심성을 자동으로 나쁜 설계라고 판정하지 않는다.
- 토큰 수를 코드 이해 비용과 동일시하지 않고 초기 근사치로 사용한다.
- 동적 언어의 모든 런타임 연결을 정적으로 완전 복원한다고 주장하지 않는다.
- 리팩터링 제안은 공개 API 안정성, 런타임 성능, 팀 관례보다 우선하지 않는다.
- Git 이력이나 agent trace가 없는 입력에서도 핵심 정적 분석은 동작해야 한다.

## 열린 질문

- 토큰 비용은 원문 토큰, AST 요약 토큰, 에이전트가 실제 읽은 범위 중 무엇으로 계산할 것인가?
- 작업의 정답과 탐색 종료 조건을 자동으로 어느 정도까지 만들 수 있는가?
- 에이전트와 모델마다 다른 탐색 정책을 저장소 수준에서 어떻게 정규화할 것인가?
- 개선 제안의 예상 효과를 실제 리팩터링 없이 어떻게 신뢰성 있게 추정할 것인가?
- 첫 benchmark baseline 이후 어떤 성공 임계값을 채택할 것인가?
