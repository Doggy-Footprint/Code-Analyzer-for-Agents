# Goal: AI Agent-Friendly Repository Analyzer

## 목적

정적 분석으로 AI 코딩 에이전트가 저장소를 탐색하고 변경할 때 불필요하게 많은 문맥과 토큰을 소비하게 만드는 구조를 찾는다. 결과는 단순한 코드 복잡도 순위가 아니라 다음 질문에 답해야 한다.

- 어떤 파일이나 심볼을 이해하려면 너무 많은 주변 코드를 읽어야 하는가?
- 탐색이 어디에서 과도하게 분기되거나 반복되는가?
- 어떤 구조가 관련 코드를 찾기 어렵게 하거나 잘못된 대상으로 유도하는가?
- 작은 변경이 넓은 문맥 확인과 검증을 요구하는 지점은 어디인가?
- 문서, 테스트, 타입, 프레임워크 지식 중 무엇을 보강하면 탐색 비용이 가장 크게 줄어드는가?

FastAPI 시각화는 이 목표를 검증하는 첫 번째 프레임워크 사례다. 최종 제품의 중심은 특정 프레임워크가 아니라 언어 수준의 분석 코어와 프레임워크별 의미 해석 계층이다.

## 현재 상태

현재 구현은 FastAPI 코드베이스의 route, dependency injection, Pydantic model 등을 추출하고 시각화하는 도구다. 아래의 중심성 지표, 토큰 비용, 작업 기반 탐색 시뮬레이션, 범용 프레임워크 계층은 아직 구현된 기능이 아니라 검증할 제품 방향과 분석 아이디어다.

## 핵심 가설

에이전트 친화성은 개별 파일의 크기나 순환 복잡도만으로 설명되지 않는다. 에이전트가 진입점에서 작업 대상과 영향 범위를 확신할 때까지 거치는 **탐색 경로의 비용, 분기, 불확실성**을 함께 측정해야 한다.

따라서 저장소를 단일 그래프가 아닌 여러 관계가 겹친 속성 그래프로 표현한다.

- 노드: 파일, 모듈, 클래스, 함수, 메서드, 타입, 설정, 테스트, 문서, API 엔드포인트
- 정적 관계: import, call, inheritance, type use, read/write, instantiate, registration
- 프레임워크 관계: route-to-handler, dependency injection, middleware, event hook, serializer/model binding
- 보조 관계: test-to-production, document-to-symbol, configuration-to-consumer, git co-change
- 노드 비용: 실제 또는 추정 토큰 수, 코드 크기, 구문 복잡도, 요약 가능성
- 엣지 신뢰도: 정적 확정, 정적 추정, 프레임워크 추론, 동적 확인 필요

인접 행렬은 분석 모델의 한 표현으로 사용할 수 있지만, 대규모 저장소에서는 희소 그래프 표현을 기본으로 한다.

## 분석 관점

### 1. 중심성과 문맥 비용의 결합

PageRank가 높으면서 읽기 비용이 큰 파일이나 심볼은 많은 작업에서 반복적으로 문맥을 소비하는 후보가 된다. 다음 지표를 함께 본다.

- **Weighted centrality cost**: PageRank 또는 eigenvector centrality × 토큰 수
- **Hub/authority score**: 참조를 넓게 분배하는 허브와 핵심 의미를 모으는 권위 노드 구분
- **Betweenness bottleneck**: 서로 다른 하위 시스템 사이의 탐색이 집중되는 브리지
- **Change blast radius**: 변경 시 확인해야 하는 역방향 의존 노드와 테스트의 비용
- **Centrality-size quadrant**: 크고 중심적인 대상, 작지만 중심적인 대상, 크지만 고립된 대상을 분리해 다른 개선책 제안

### 2. 에이전트 탐색 시뮬레이션

단순 2-hop/3-hop 노드 수 대신 실제 작업을 흉내 낸 예산 제한 탐색을 수행한다.

1. 작업 단서에서 시작 노드를 고른다. 예: URL, 심볼명, 오류 메시지, 설정 키, 변경 파일.
2. import, call, type, test, 프레임워크 엣지를 우선순위에 따라 탐색한다.
3. 방문 노드의 토큰 비용과 선택하지 않은 분기의 수를 누적한다.
4. 구현 지점, 영향 범위, 관련 테스트를 충분한 신뢰도로 찾으면 종료한다.

이 과정에서 다음을 측정한다.

- **k-hop context cost**: 1/2/3-hop 이웃을 읽는 데 필요한 중복 제거 토큰 수
- **Branching burden**: 각 단계의 fan-out과 관련 없는 후보 비율
- **Target discovery cost**: 시작 단서에서 정답 심볼까지의 최소 가중 비용
- **Impact discovery cost**: 변경 대상에서 호출자, 소비자, 테스트까지 확인하는 비용
- **Context fragmentation**: 필요한 정보가 몇 개 파일과 디렉터리에 흩어져 있는지
- **Backtracking risk**: 이름 충돌, 재수출, 간접 등록 등으로 잘못된 경로를 선택할 가능성
- **Evidence gap**: 정적 분석만으로 확정할 수 없어 추가 검색이나 실행이 필요한 경계

모든 이웃을 동일하게 읽는 모델과 최단 경로만 찾는 모델 사이의 편향을 줄이기 위해 BFS, 가중 최단 경로, 확률적 탐색 정책을 비교한다.

### 3. 구조적 마찰 탐지

다음 패턴을 에이전트 탐색 효율 저하 후보로 보고 근거와 함께 표시한다.

- 여러 책임이 모인 중심 대형 파일 또는 god symbol
- 과도한 barrel/re-export와 동일·유사 이름으로 인한 심볼 해소 모호성
- 동적 import, reflection, monkey patching, 문자열 기반 등록처럼 정적 연결이 끊기는 지점
- 정의와 등록, 설정과 소비, 구현과 테스트가 멀리 떨어진 구조
- 공통 유틸리티 허브가 관련 없는 도메인을 연결하는 구조
- 순환 의존과 양방향 호출로 탐색 종료 지점이 불명확한 구조
- 프로덕션 코드와 테스트의 연결 누락 또는 지나치게 큰 통합 테스트만 존재하는 구조
- 실제 구조와 README, 경로명, 타입 힌트가 불일치하는 경우
- 생성 코드, vendor 코드, migration처럼 비용은 크지만 일반 작업에서 우선순위가 낮은 영역이 탐색을 오염시키는 경우

탐지 결과는 곧바로 리팩터링 결론으로 취급하지 않는다. 각 결과에 작업 유형, 근거 경로, 오탐 가능성을 함께 제공한다. 예상 절감 토큰은 실제 trace로 보정할 수 있을 때만 실험 지표로 제공한다.

### 4. 정적 그래프와 변경 이력의 불일치

정적 의존 관계와 git co-change 관계를 비교하면 코드에 드러나지 않은 결합을 찾을 수 있다.

- 함께 자주 바뀌지만 정적 경로가 없는 파일: 숨은 규약이나 문서화되지 않은 결합 후보
- 강하게 연결됐지만 함께 바뀌지 않는 파일: 안정된 인터페이스 또는 과대 추정된 영향 범위 후보
- 한 변경이 여러 커뮤니티를 반복해서 건드리는 경우: 경계 또는 책임 분리 문제 후보

git이 없는 입력에서도 핵심 정적 분석은 동작해야 하며, 이력 기반 신호는 선택적 보강 정보로 취급한다.

## 프레임워크 확장 모델 후보

현재 아이디어를 구현할 때 다음 계층 분리를 우선 검토한다. 실제 경계는 프로토타입으로 결합도와 확장성을 확인한 뒤 확정한다.

1. **언어 코어**: AST, 심볼 테이블, import/call/type 그래프, 소스 범위와 토큰 비용
2. **프레임워크 어댑터**: FastAPI의 route/Depends처럼 일반 Python 의미만으로 복원하기 어려운 연결
3. **저장소 보강기**: 테스트 연결, 문서 링크, 설정 소비, git co-change
4. **작업 모델**: 버그 수정, 기능 추가, API 변경, 설정 변경 등 과제별 시작점과 종료 조건
5. **진단 및 시각화**: 비용이 큰 경로, 병목, 개선 후보와 근거 제시

FastAPI 분석은 프레임워크 전용 제품이 아니라 어댑터 인터페이스가 실용적인지 검증하는 기준 사례로 사용한다. Django, Flask, Typer/Celery 같은 등록·DI 중심 프레임워크에 동일한 모델을 적용할 수 있는지는 이후 검증한다.

## 결과물 후보

분석 결과는 점수 하나보다 행동 가능한 증거를 우선한다.

- 저장소 전체의 탐색 비용 지도와 상위 병목
- 파일·심볼별 중심성, 토큰 비용, fan-in/fan-out, 커뮤니티, 신뢰도
- 대표 작업별 탐색 경로와 누적 토큰 비용
- "이 파일을 분리하라"보다 구체적인 개선 후보: 진입점 문서화, 이름 변경, 타입 보강, re-export 축소, 테스트 인접성 개선, 프레임워크 연결 명시 등
- 변경 전후 또는 브랜치 간 agent-friendliness diff
- JSON 출력과 사람이 검토할 수 있는 대화형 리포트

## 검증 아이디어

지표가 보기 좋은 숫자에 그치지 않도록 실제 에이전트 행동과 대조한다.

- 여러 공개 저장소에 동일한 작업 세트를 만들고 정답 파일, 영향 파일, 테스트를 라벨링한다.
- 허용되는 환경에서는 실제 코딩 에이전트의 파일 열기·검색·토큰 사용·되돌아가기 trace를 익명화해 검증 데이터로 사용할 수 있다.
- 예측한 탐색 비용과 실제 읽은 토큰, 도구 호출 수, 해결 시간, 성공률의 상관을 측정한다.
- 저장소 크기, 언어, 프레임워크, 작업 유형별로 결과를 분리한다.
- 휴리스틱별 ablation으로 PageRank, 토큰 가중치, 프레임워크 엣지, git 신호가 실제 예측력을 높이는지 확인한다.
- 개선 제안 적용 전후에 같은 작업을 재실행해 인과적인 비용 감소가 있는지 확인한다.

초기 성공 기준은 특정 점수의 극대화가 아니라, 높은 비용으로 예측한 경로가 실제 에이전트 탐색에서도 반복적으로 비싸고 도구가 그 이유를 재현 가능한 경로로 설명하는 것이다.

## 비목표와 주의점

- 전통적인 코드 품질, 보안, 런타임 성능 분석을 대체하지 않는다.
- 큰 파일이나 높은 중심성을 자동으로 나쁜 설계라고 판정하지 않는다.
- 토큰 수를 코드 이해 비용과 동일시하지 않고 초기 근사치로 사용한다.
- 동적 언어의 모든 런타임 연결을 정적으로 완전 복원한다고 주장하지 않는다.
- 리팩터링 제안은 공개 API 안정성, 런타임 성능, 팀 관례보다 우선하지 않는다.

## 제안 단계

### 1단계: 측정 가능한 코어

- Python 파일·심볼 그래프와 희소 그래프 저장 형식
- import/call/type/containment 엣지
- 소스 범위 기반 토큰 비용 근사
- PageRank, hub, betweenness, k-hop 비용, 커뮤니티 분석
- 분석 제외 규칙과 신뢰도 표시

### 2단계: 작업 기반 탐색

- URL, 심볼명, 오류 문자열, 변경 파일을 시작점으로 하는 대표 작업 모델
- 탐색 정책별 비용 비교
- 테스트 및 영향 범위 발견 비용
- 비용이 큰 실제 경로를 보여 주는 리포트

### 3단계: 의미 보강과 검증

- FastAPI 어댑터를 일반 플러그인 인터페이스로 분리
- git co-change와 문서·테스트 연결
- 실제 에이전트 trace 기반 지표 보정
- 변경 전후 architecture/agent-efficiency diff

## 열린 질문

- 토큰 비용은 원문 토큰, AST 요약 토큰, 에이전트가 실제 읽은 범위 중 무엇으로 계산할 것인가?
- 작업의 정답과 탐색 종료 조건을 자동으로 어느 정도까지 만들 수 있는가?
- 에이전트와 모델마다 다른 탐색 정책을 하나의 저장소 지표로 어떻게 정규화할 것인가?
- 프레임워크 어댑터가 제공해야 할 최소 인터페이스와 엣지 신뢰도 규칙은 무엇인가?
- 개선 제안의 예상 효과를 실제 리팩터링 없이 어떻게 신뢰성 있게 추정할 것인가?
