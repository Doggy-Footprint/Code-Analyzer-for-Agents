# Goal: Task Difficulty and Structural Cost Analyzer

## 목표

정적 분석으로 AI 코딩 에이전트가 저장소를 탐색하고 변경할 때 비효율적인 탐색이 이루어지는 병목 지점을 찾는다. 분석 방식과 제공하는 리포트는 다음과 같다.

1. 저장소를 그래프 구조로 설명하고, 그래프 및 인접 행렬 분석에서 도출한 지표를 활용한다.
2. 작업을 해결하기 위해 필요한 최소 탐색 비용을 추정하고, 최소 탐색 비용을 높이는 구조적 병목을 탐지한다. 이는 AI agent가 최대한 효율적인 탐색을 하는 것을 가정한다.
3. 작업을 해결하기 위해 필요한 최대 탐색 비용을 추정하고, 최대 탐색 비용을 높이는 구조적 병목을 탐지한다. 이는 AI agent가 비효율적인 탐색을 하는 것을 가정한다. 여기서 비효율적인 것은 탐색 대상을 실제로 확인하기 전에 결정 지을 수 없고, 그러한 탐색에서 타겟을 늦게 발견하는 것을 말한다.
4. 작업을 해결하기 위해 필요한 기대 탐색 비용을 추정한다. 이는 위 3의 전제, 즉 읽기 전에는 후보를 구별할 수 없다는 가정을 그대로 적용해 탐색 순서를 균등 무작위로 두었을 때의 기대값이다.
5. 같은 저장소 및 실행 조건에서 코드 탐색시 정답 지정과 수정 영향 범위를 찾는데 필요한 관측 범위가 큰 작업을 예측하고, 이를 작업 간 상대적 난이도 순위로 제공한다.

이에 따라 정적 분석은 일반적인 의미에 더해 AI agent에게 영향을 줄 수 있는 주석 등을 포함한다. 문서의 경우는 정적 분석으로 판별이 어렵기에 별도로 다룬다.

### 부가 목표

아래는 분석 과정에서 목표로 삼을 수 있는 대상들이다. 메인 목표의 계산 결과를 재사용해 답하며, 별도의 예측 모델을 두지 않는다.

- 어떤 파일이나 심볼을 이해하려면 너무 많은 주변 코드를 읽어야 하는가?
- 어떤 구조가 관련 후보를 과도하게 노출하거나, 근거의 반복 확인을 요구하여, AI agent의 탐색 및 작업 비용을 높이는가?
- 어떤 구조가 관련 코드를 찾기 어렵게 하거나 잘못된 대상으로 유도할 가능성이 있는가?
- 작은 변경이 넓은 문맥 확인과 검증을 요구하는 지점은 어디인가?
- 문서, 테스트, 타입, 프레임워크 지식 중 무엇을 보강하면 탐색 비용이 가장 크게 줄어드는가?

각 질문에 어떤 산출물이 답하는지는 [부가 산출물](#부가-산출물)에서 다룬다.

## 핵심 출력 계약

| 출력 | 입력 | 결과 | 비교 범위 |
|---|---|---|---|
| 상대적 난이도 순위 | 저장소 그래프, 동일한 작업 세트, 비용 모델 | 작업별 순위와 pairwise 비교 근거 | 같은 저장소·언어·작업 조건이 기본 |
| 최소 탐색 비용 | 작업 seed, 정답 집합, 종료 조건 | 최저 비용, 경로, 필요한 파일·심볼 | 작업별 |
| 기대 탐색 비용 | 위 입력과 후보 집합, 균등 순서 가정 | 기대 비용과 계산에 사용된 후보·계수 | 작업별 |
| 최대 탐색 비용 | 위 입력과 후보 집합 | 최악 비용, 늦게 발견되는 타겟, 노출된 비타겟 후보 | 작업별 |
| 구조적 병목 | 세 비용의 경로·후보 집합, 그래프 구조 | 병목 노드·엣지, 세 비용 각각에 대한 추가 비용, 신뢰도 | 작업별 및 반복 출현 집계 |
| 예측 신뢰도 | 엣지 해소 상태, 정답 완성도, 모델 보정 결과 | 신뢰 구간 또는 등급과 한계 | 각 예측 |

순위에는 반드시 점수 차이와 근거가 포함되어야 한다. 차이가 불확실성보다 작으면 강제로 순서를 정하지 않고 동순위 또는 판정 보류로 표시한다.

## 분석 모델

저장소를 여러 관계가 겹친 희소 속성 그래프로 표현한다.

- 노드: 파일, 모듈, 클래스, 함수, 메서드, 타입, 설정, 테스트, 문서, API 엔드포인트
- 정적 관계: import, call, inheritance, type use, read/write, instantiate, registration
- 프레임워크 관계: route-to-handler, dependency injection, middleware, event hook, serializer/model binding
- 보조 관계: test-to-production, document-to-symbol, configuration-to-consumer, Git co-change
- 노드 비용: 글자 수, 코드 크기, 구문 복잡도, 요약 가능성
- 엣지 속성: 관계 종류, 신뢰도, 해소 상태, 탐색 비용

작업은 seed, 정답 집합, 종료 조건으로 정의한다. 버그 수정은 결함 심볼과 회귀 테스트, 기능 추가는 확장 지점과 인터페이스, API 변경은 소비자와 호환성 검증 지점, 설정 변경은 모든 소비자와 테스트를 정답 집합으로 삼는다.

## 난이도 예측 모델

초기 예측 단위는 작업이다. 다음 신호를 사용하되, 가중치는 검증 데이터셋으로 보정한다.

- 최소 탐색 비용과 최소 경로 길이
- 기대 탐색 비용과 기대 경로 길이
- 최대 탐색 비용과 최대 경로 길이
- 최소와 최대의 폭, 즉 탐색 순서에 따른 비용 변동성
- 후보 분기 수와 되돌아가기 위험
- 필요한 파일·디렉터리의 분산도
- 영향 범위와 요구 테스트의 수
- ambiguous, unresolved, dynamic-required 관계 비율
- 병목 노드·엣지의 수와 우회 비용

평가는 절대 비용 오차보다 순위 품질을 우선한다.

- 전체 순위: Spearman 또는 Kendall 순위상관
- 두 작업 비교: pairwise accuracy와 동률 판정 품질
- 상위 난이도 작업: top-k precision/recall
- 보조 지표: 실제 토큰, 도구 호출 수, 해결 시간, 성공률에 대한 오차

서로 다른 저장소 간 순위는 규모, 언어, 작업 유형, 에이전트 환경의 정규화가 검증되기 전까지 제공하지 않는다.

## 구조적 병목 정의

구조적 병목 후보는 다음 조건을 작업별로 평가한다.

- 많은 유효 경로가 통과하는 articulation·bridge 성격의 노드 또는 엣지
- 제거하거나 비용을 낮췄을 때 최소 비용이 크게 감소하는 대상
- 구별 불가능한 비타겟 후보를 대량으로 노출해 기대 비용과 최대 비용을 함께 올리는 대상
- 우회 경로의 비용이 현저히 큰 대상
- 모호성·미해소·동적 연결 때문에 추가 검색을 요구하는 경계
- 정의, 소비자, 설정, 테스트가 멀리 분산된 연결
- 여러 고난도 작업에서 반복해서 나타나는 공통 병목

각 진단은 영향을 받는 작업, 최소 경로, 대안 경로, 세 비용 각각에 대한 추가 비용, 엣지 신뢰도와 오탐 가능성을 제시해야 한다. 최소 비용만 올리는 병목과 기대·최대 비용만 올리는 병목은 완화 방법이 다르므로 구분해 보고한다.

## 현재 기반

| 상태 | 범위 |
|---|---|
| 구현됨 | Python, TypeScript/JavaScript, Kotlin 파일·심볼 그래프와 언어 관계 |
| 구현됨 | FastAPI 및 Android Compose, DI, Room, Retrofit 의미 분석 |
| 구현됨 | PageRank, HITS, degree/betweenness, 토큰 비용, 2/3-hop 비용 |
| 구현됨 | 작업 정의, 정답 집합, 작업 seed retrieval |
| 기구현됨 | 중심 대형 심볼, 브리지, re-export 모호성, 순환 의존, 테스트 연결 누락 진단 |
| 구현됨 | 두 분석 상태의 노드 비용·진단 diff |
| 미구현 | 최소·기대·최대 비용의 공통 계약과 후보 집합 정의 |
| 검증 필요 | 작업 순위 모델, 병목의 인과적 비용 기여도 |

## 로드맵

### M0. 그래프 및 작업 탐색 기반 — 완료

- 언어·프레임워크 그래프와 공통 노드·엣지 모델
- 토큰 비용, 중심성, k-hop 비용
- 작업 정의, seed retrieval, 정답 집합
- JSON, Mermaid, HTML 및 상태 간 diff

완료 조건: 동일 입력에서 결정적인 그래프와 작업 seed 해석을 재현한다.

### M1. 최소·기대·최대 비용 기준선 — 다음

- 단일 목표와 다중 목표에 대한 세 비용의 공통 계약 확정
- 각 탐색 단계에서 노출되는 후보 집합과 비타겟 집합의 결정적 정의 - 재검토
- 중복 읽기를 제거한 고유 노드 비용과 요구 recall 처리
- 균등 계수 적용과 최소·최대와의 일관성 검증
- 신뢰도가 낮은 엣지의 낙관·기준·비관 비용 범위
- 작은 fixture의 완전 탐색 결과와 알고리즘 결과 대조
- 최대, 최소 비용과 기대 비용의 gap 출력

완료 조건: 모든 계약 fixture에서 세 비용과 경로가 검증된 정답과 일치하고, `최소 ≤ 기대 ≤ 최대`가 항상 성립하며, 동일 비용 경로와 도달 불가능 상태를 결정적으로 처리한다.

### M2. 작업 난이도 순위 baseline — 계획됨

- 실제 저장소에서 작업, seed, 정답 파일·심볼, 영향 범위, 테스트 라벨링
- 세 비용, 비용 폭, 분기, 분산, evidence gap 기반 baseline 모델
- pairwise 및 전체 순위 평가와 동순위·판정 보류 정책
- 저장소·언어·작업 유형별 교차 검증과 신호 ablation

완료 조건: 검증 데이터셋의 선정·버전·평가 방법을 먼저 확정하고, 누수 없는 평가에서 단순 기준선보다 나은 순위 품질과 채택 임계값을 재현한다.

### M3. 작업별 구조적 병목 탐지 — 계획됨

- 최소 경로상의 필수 노드·엣지와 대안 경로 계산
- 비타겟 후보를 대량 노출하는 지점의 기대·최대 비용 기여도 계산
- 노드·엣지 제거 또는 비용 감소에 대한 민감도 분석
- 병목별 추가 비용과 영향을 받는 작업 집계
- 기존 구조 진단을 작업별 비용 기여도와 연결
- 정적 근거가 부족한 병목의 신뢰도 하향 및 검증 방법 제시

완료 조건: 각 병목이 어떤 작업의 최소·기대·최대 비용을 각각 얼마나 높이는지 재현 가능한 경로와 반사실 비교로 설명한다.

### M4. 변경 효과와 우선순위 — 계획됨

- 변경 전후 동일 작업 세트의 순위와 세 비용 비교
- 병목 완화 후보별 예상 비용 감소와 영향 작업 수 집계
- 그래프의 병목과 지표를 개선 후보에 연결한 설명 제공
- 실제 변경 없이 가능한 제한적 what-if 분석
- 비용 감소, 적용 범위, 예측 신뢰도를 분리해 표시

완료 조건: 개선 전후 검증 데이터에서 방향성이 반복 확인된 항목만 우선순위 후보로 제시한다.

### M5. 범위 확장과 운영화 — 보류

- Git co-change, document-to-symbol, 문서·코드 불일치 신호
- 추가 언어·프레임워크 어댑터
- 대규모 그래프 근사 알고리즘과 성능 예산
- 검증 데이터셋 버전 관리와 회귀 평가

완료 조건: 확장이 기존 비용·순위 계약을 바꾸지 않고 동일한 평가 파이프라인을 재사용한다.

## 부가 산출물

부가 목표의 질문에는 메인 목표를 계산하며 얻은 값을 재구성해 답한다. 새로운 예측 모델을 도입하지 않으며, 각 산출물은 근거가 되는 노드·엣지·경로로 역추적할 수 있어야 한다.

| 부가 목표 질문 | 답하는 산출물 | 근거가 되는 메인 계산 | 마일스톤 |
|---|---|---|---|
| 이해에 필요한 주변 코드의 양 | 노드별 문맥 비용과 k-hop 비용 | 노드 비용, k-hop 비용 | M0 완료 |
| 후보 과다 노출과 근거 반복 확인 | 분기 부담과 비타겟 노출량 리포트 | 기대·최대 비용의 비타겟 후보 집합 | M1 |
| 탐색을 어렵게 하거나 오도하는 구조 | 모호성·미해소 연결 진단 | evidence gap, 엣지 신뢰도 | M3 |
| 작은 변경이 넓은 검증을 요구하는 지점 | 영향 범위 확산 지점 | impact discovery cost, 병목 민감도 | M3 |
| 무엇을 보강하면 비용이 가장 줄어드는가 | 개선 후보 우선순위 | 병목 완화 반사실 비교 | M4 |

**그래프 기반 설명.** 그래프는 시각화인 동시에 설명의 근거다. 저장소의 핵심 구조, 지표의 의미, 병목과 연결된 개선 후보를 함께 제시하며, 모든 항목은 관련 노드·엣지·경로로 역추적할 수 있어야 한다. 개선 후보는 어떤 지표를 왜 바꿀 것으로 예상하는지와 영향을 받는 작업을 함께 제시한다.

## 예상 사용자 흐름

1. 저장소와 비교할 작업 세트를 분석한다.
2. 예상 난이도 순위와 판정 신뢰도를 확인한다.
3. 작업별 최소·기대·최대 비용과 근거 경로를 확인한다. 
4. 사용자의 필요와 주요 작업 유형에 따라 실질적 병목을 식별한다.
5. 지표와 그래프 근거가 연결된 개선 후보를 검토한다.
6. 개선 후에 같은 작업 세트로 순위와 비용 변화를 비교한다.

## 검증 원칙

- 학습·보정 데이터와 최종 평가 저장소를 분리한다.
- 같은 작업 조건에서 예측 순위와 실제 관측 순위를 비교한다.
- 시간, 모델, 도구 설정과 캐시 상태를 기록한다.
- 휴리스틱별 ablation으로 추가 예측력을 확인한다.
- 세 비용은 항상 `최소 ≤ 기대 ≤ 최대`를 만족하는지 검사하고, 위반은 계약 결함으로 처리한다.
- 병목은 상관관계만이 아니라 제거·완화 전후의 비용 변화로 검증한다.
- 결과는 점수뿐 아니라 경로, 비용 구성, 불확실성을 함께 제공한다.

## 비목표

- 작업의 의미적 구현 난이도나 개발자의 숙련도를 완전히 예측하지 않는다.
- 서로 다른 실행 환경의 작업을 보정 없이 직접 비교하지 않는다.
- 전통적인 코드 품질, 보안, 런타임 성능 분석을 대체하지 않는다.
- 높은 중심성, 큰 파일, 순환 의존을 자동으로 결함으로 판정하지 않는다.
- 동적 런타임 연결을 정적으로 완전 복원한다고 주장하지 않는다.
- 최소 탐색 비용을 실제 에이전트 비용이나 전체 작업 비용과 동일시하지 않는다.
- 기대 비용을 특정 에이전트의 실제 비용 예측치로 제시하지 않는다.

## 열린 질문

- 실제 난이도의 기준값을 토큰, 도구 호출, 시간, 성공률 중 어떻게 결합할 것인가?
- 다중 목표 작업의 요구 recall과 종료 조건을 누가 어떻게 라벨링할 것인가?
- 각 탐색 단계에서 "노출된 후보"의 경계를 어디까지로 볼 것인가?
- 동일 비용의 작업을 동순위로 판정할 최소 차이와 불확실성 기준은 무엇인가?
- 최소·기대·최대 비용 계산에서 동적·미해소 엣지의 비용 범위를 어떻게 보정할 것인가?
- 병목 완화의 반사실 효과를 코드 변경 없이 어느 수준까지 신뢰할 것인가?
- 실제 저장소 소스와 완료된 작업을 이용한 검증 데이터셋의 선정·버전·평가 방법은 어떻게 정할 것인가?
