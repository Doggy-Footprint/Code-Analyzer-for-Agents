A. 실제 AI 동작에는 있는데 모델에 빠진 것 (결정적·저비용인데도 누락)

A1. 파일 트리 탐색이 아예 없다 — 가장 큰 격차
문서의 탐색 원자는 query(내용 검색) → read 뿐입니다. 실제 에이전트가 task를 받고 가장 먼저 하는 행동은 grep이 아니라 ls / glob (**/*Settings*.kt) / 경로 훑기입니다. 경로·파일명 자체가 검색 가능한 네임스페이스인데, ROADMAP은 "파일·모듈 경로"를 파일 내용 안에 적힌 문자열로만 다룹니다.
- 누락 결과: feature/settings/AudioSettingDialog.kt는 실제로는 seed query 한 번에 파일명으로 잡히는데, 모델에서는 내용 검색으로만 도달 가능해 비용이 과대평가됩니다.
- 구현 난이도: 낮음. 디렉터리 노드(토큰 비용 ≈ 0인 readable node) + 경로 토큰 매칭 query 종류 추가. derived query 규칙표를 경로에 그대로 재사용하면 됩니다.

A2. import / qualified name 해석에 query 노드가 붙는다
문서는 "무검색 read 1회 이동"을 framework rule에만 허용합니다. 그런데 import a.b.Foo를 읽은 에이전트는 검색하지 않고 바로 해당 파일을 엽니다. 이건 framework 규칙과 동일하게 "도착 노드를 유일하게 특정"하는 경우입니다.
- 현 모델대로면 import 추종이 grep Foo(결과 40개)와 같은 비용이 되어, 언어 자체의 해석 가능성이 주는 이점이 전부 사라집니다.
- 구현 난이도: 낮음. "유일 특정 / 후보 축소" 이분법을 framework 절이 아니라 연결 일반 속성으로 승격하면 됩니다.

A3. 진입 문서(README, AGENTS.md, index.md)가 특권 노드가 아니다
실제 에이전트는 검색 이전에 루트 README·AGENTS.md·디렉터리 index를 읽습니다. 이 저장소의 CLAUDE.md 자체가 index.md를 강제하고 있는데, ROADMAP에서는 문서가 그냥 occurrence 표면일 뿐입니다.
- 이미 읽었을 문서들의 처리

A4. 파일 전체 흡수 규칙에 크기 상한이 없다
"한 symbol을 읽으면 같은 파일 모든 symbol을 읽은 것으로 처리"는 300줄 파일에는 맞지만 8000줄 God file에는 틀립니다. 실제 에이전트는 큰 파일을 offset/limit으로 잘라 읽거나 파일 안에서 다시 grep합니다.
- 현 규칙은 거대 단일 파일을 가장 저렴한 구조로 평가합니다. 병목을 드러내겠다는 목적과 정면으로 반대 방향입니다.
- 구현 난이도: 낮음. 임계 토큰 N 이하 = 전량 흡수, 초과 = 부분 read(청크 단위 흡수). N은 지금 정해야 합니다(→ B2).

A5. 결과 순서를 균등 무작위로만 본다
기대 비용이 "매 단계 query와 결과 순서를 무작위 선택"입니다. 실제 에이전트는 grep 출력 줄만 보고도 class Foo(선언)와 Foo(...)(사용)를 구분해 선언부를 먼저 엽니다. 경로 휴리스틱(test 후순위, 파일명 완전 일치 우선, vendor 제외)도 항상 씁니다.
- 이 정보는 검색 시점에 공짜로 이미 있는데 모델이 버립니다. 기대 비용이 실제보다 크게 비관적으로 나옵니다.
- 구현 난이도: 낮음. occurrence에 선언/참조 종류만 태깅하고, 결정적 우선순위 prior를 profile 파일에 둡니다(이미 profile 관리 체계가 있음).

A6. 50개 초과 query를 "제외"하는 건 실제 동작이 아니다
실제 에이전트는 결과가 폭발하면 query를 버리는 게 아니라 좁힙니다: 경로 필터 추가, 토큰 조합, 확장자 제한. 문서 규칙("제외하고, 유일 경로였으면 도달 불가")은 도달 가능한 노드를 인위적으로 unreachable로 만듭니다.
- 구현 난이도: 낮음. derived query 규칙표에 "기존 query + 경로/확장자 한정"을 정식 변형으로 추가하고, 좁힌 query가 50 이하로 떨어지면 살립니다.

A7. 결과 그룹 소진 규칙이 실제 동작과 반대인데 근거가 없다
"읽은 노드에서 나온 새 query는 현재 그룹을 다 확인할 때까지 대기"는 세 시나리오 전부에 걸리는 하드 제약입니다. 실제 에이전트는 강한 단서를 얻는 즉시 현재 그룹을 버리고 pivot(depth-first)합니다.
- 이 결정이 최소/기대/최대 비용 전부의 값을 지배하는데 문서에 근거가 한 줄도 없습니다. 되돌리기 비싼 결정 + 대안(best-first pivot)이 명확히 존재 → ADR 대상입니다. 최소한 탐색 정책을 파라미터로 두고 기본값만 BFS-exhaust로 고정하길 권합니다.

A8. 병렬 tool call과 0-result query가 비용 축에 없다
- 실제 에이전트는 한 turn에 grep 여러 개를 동시에 던집니다. 문서의 "탐색 turn"은 완전 직렬이라, "실제 agent 로그로 보정한다"는 검증 원칙과 축 자체가 대응되지 않습니다. 최소한 직렬 등가 turn이며 실제 turn 수와 비교 대상이 아님을 명시해야 합니다.
- seed query는 0-result가 흔한데(sLLM이 저장소에 없는 단어 생성), 이건 turn과 tool call을 소모합니다. 비용 축에 "0-result query 수"가 없습니다. 추가는 자명하게 쉽습니다.

A9. 종료 조건이 오라클이다
"모든 target을 읽으면 종료"는 실제 누락(에이전트가 일부만 찾고 멈춤)을 발견 단계에서 표현할 수 없게 만듭니다. 누락 위험이 zone 단계에만 존재하게 됩니다.
- 쉬운 보완: 비용 축에 target별 발견 turn 인덱스와 검색 결과에 노출됐지만 읽지 않은 target 수를 기록. 조기 종료 위험을 사후 도출할 수 있습니다.

B5. query 동등성과 중복 실행 — 서로 다른 노드가 같은 문자열을 만들면 같은 query node인가? 이미 실행한 query가 대기열에 다시 들어오면? 이 규칙이 없으면 비용이 비결정적이 되어 M1 완료 조건(재현성)을 정의할 수 없습니다.

ZONE이 적절한 범위에서 끝나는지 체크가 필요함



---

quick fix


토크나이저 = 문자 * 3배수
50개 하드 제약
저장소에서 제외할 것들 - lock, binary, build, generated


---

1. 힌트를 그래프에서 어떻게 정의하나 (A7)

노드가 아니라 엣지 속성으로 둡니다. 문서가 "occurrence는 독립 readable node가 아니다"라고 정한 건 맞는데, 그 결정 때문에 occurrence가 가진 정보까지 같이 버려진 게 문제입니다. 힌트는 도착점이 아니라 query node → readable node 엣지에 붙는 정보입니다.

정의: 힌트 = 검색 도구의 출력 형식이 occurrence에서 노출하는 결정적 투영(projection). 즉 hint = f(occurrence, output_format)이고, output_format(매치 줄만, 앞뒤 N줄, 파일명만)은 계약에서 고정하는 파라미터입니다. 그래야 결정성이 유지됩니다.

엣지에 실리는 값:
- 도착 노드의 경로·파일명·symbol 이름
- occurrence의 구문 역할: 선언 / import / 호출 / 문자열 리터럴 / 주석 / 문서 mention / 테스트
- 노출된 줄 window 안의 identifier 집합

중요한 건 힌트가 이미 지불된 정보라는 점입니다. "query 결과 token" 축에서 비용을 이미 내고 있는데 모델이 쓰지 않을 뿐입니다. 새 비용 축이 필요 없습니다.

힌트가 하는 일 세 가지:
1. 읽기 순서 prior (A5) — 선언 occurrence를 사용 occurrence보다 먼저 읽음.
2. read 없는 query 생성 — 이게 구조적으로 제일 큽니다. 실제 에이전트는 grep 결과 줄에 class SettingsViewModel(private val audioRepo: AudioRepository)가 보이면 파일을 열지 않고 바로 AudioRepository를 grep합니다. 즉 query → query 엣지가 read를 거치지 않고 존재합니다. 현재 모델은 모든 새 query가 read에서만 나오므로 이 경로를 표현할 수 없습니다.
3. 누락 위험 측정 (A9) — "힌트는 받았으나 읽지 않은 노드"가 정의됩니다. 이게 조기 종료 위험의 관측 가능한 형태입니다.

폭발 방지 규칙 하나는 지금 필요합니다: query 생성 후보는 노출된 줄 window 안의 identifier로만 한정. window가 계약으로 고정되어 있으므로 자연히 유계입니다.

2. B4 (tie-break) 구체적으로

왜 사소하지 않은지부터. 최소 비용은 값이 하나지만 그 값을 내는 경로는 여럿입니다. weight가 "명시적으로 임시"라서 동률이 오히려 흔합니다. 그런데 M4는 병목을 min_path 위의 node/query/edge에 귀속시키고, min_path는 출력 계약의 역추적 필드입니다. 즉 tie-break 규칙 하나가 병목 순위와 what-if 결과, 그리고 run 간 diff 노이즈를 결정합니다. "결정적으로 처리한다"는 문장은 결정성만 보장하고 어떤 결정인지는 안 정한 상태입니다.

제안하는 형태는 가중합이 동률일 때만 적용되는 사전식 cascade입니다:

1. 원 비용 축의 사전식 비교. 축 우선순위를 profile 파일에 명시 (예: 탐색 turn → search call → read call → query 결과 token → node token → 노출 비타겟 수). weight와 별개로 고정하는 게 핵심입니다. weight를 교체해도 tie-break는 안 흔들려야 profile 교체 전후 비용 비교가 의미를 갖습니다.
2. 경로 길이(엣지 수)가 짧은 쪽.
3. id 시퀀스의 사전식 최소. 이때 id는 안정적이어야 합니다 — 정규화된 저장소 상대 경로 + qualified symbol name, 구분자 정규화, 바이트 순서 비교. 파일시스템 열거 순서나 해시에 의존하면 M1의 결정적 serialization이 깨집니다.

그리고 규칙 자체를 profile처럼 버전 붙여 출력에 기록하고, M1의 결정성 테스트 범위에 포함시켜야 합니다.

추가로 권하는 것: tie-break로 하나를 고르는 것과 별개로 동률 경로 개수를 출력에 남기세요. "동률 3개 중 선택"이 기록되면, M4가 "이 노드가 병목이다"라고 말할 때 그게 실제 집중인지 tie-break 우연인지 구분됩니다. 병목 귀속은 이상적으로는 단일 min_path가 아니라 동률 경로 집합 전체를 봐야 하고, 그러면 tie-break는 표시용으로 격하되어 위험도 자체가 줄어듭니다.